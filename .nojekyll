```plaintext name=.nojekyll

```

Push .nojekyll to the main branch with the commit message "Add .nojekyll for GitHub Pages support" in repository lidska90/Btc-priceweb with contents: (empty file as above).
Push .nojekyll to the main branch with the commit message "Add .nojekyll for GitHub Pages support" in repository lidska90/Btc-priceweb with contents:

```plaintext name=.nojekyll

```
